// Deprecated Prisma 7 config file retained temporarily to avoid build errors.
// Using standard Prisma 5.x schema configuration (datasource url in schema.prisma).
// This file is now a no-op and can be deleted once all references are cleared.
export {};
